# 課題・未実装一覧

SpreadSheet / Excel と比較した際の機能差分と、実装上の既知バグを整理する。
マージ済みの対応を反映し、ユーザーフィードバックによる新規課題を追加。

---

## 1. GUI — セル編集体験

### 1-1. ~~セル内直接編集ができない~~ ✅ 対応済み

### 1-2. ~~コピー＆ペースト（単セル・TSV）~~ ✅ 対応済み（Ctrl+C / Ctrl+V）

### 1-3. 複数セル・行の選択コピーができない ⚠️ **未対応**

現状は単一フォーカスセルのコピーのみ。

- **期待動作**: Shift+クリックや Shift+矢印で範囲選択し、Ctrl+C で TSV としてコピー
- AG Grid の範囲選択（`enableRangeSelection`）は Enterprise 機能のため、手動実装が必要
- 選択行全体のコピー（行選択 → Ctrl+C → TSV）も未対応

### 1-4. セルを同じ値で埋める機能がない ⚠️ **未対応**

- **フィルダウン（Ctrl+D）**: 上のセルの値を選択範囲全体にコピー
- **同一値フィル**: 右クリック → 「選択範囲を同じ値で埋める」
- 範囲選択が前提になるため 1-3 と連動

---

## 2. GUI — 行操作

### 2-1. ~~行の追加ができない~~ ✅ 対応済み（ツールバーボタン・右クリック・id 自動採番）

### 2-2. ~~行の削除ができない~~ ✅ 対応済み（Delete キー・右クリック）

### 2-3. 行の並び替えができない ⚠️ **未対応**

ドラッグ＆ドロップによる行順変更未実装（AG Grid の `rowDragManaged` で対応可能）。

---

## 3. GUI — 色・コメント（RichCell）操作

### 3-1. 色・コメントの入力が右クリックからできない ⚠️ **未対応**

現状はグリッド下部の「セル詳細パネル」でのみ編集可能。
右クリックメニューから直接設定できるとより直感的。

- 右クリック → 「色を設定」→ サブメニューで色選択
- 右クリック → 「コメントを編集」→ インラインテキスト入力

### 3-2. コメント付きセルの視覚的マーカーがない ⚠️ **未対応**

DESIGN.md §9.3 では「右上に三角マーク（Excel 風）」と定義されているが未実装。
`comment` が存在するセルを一目で識別できない。

AG Grid の `cellRenderer` でカスタム描画が必要。

---

## 4. GUI — フォルダ・セッション管理

### 4-1. 最後に開いたフォルダを記憶できない ⚠️ **未対応**

毎回「フォルダを開く」でピッカーを操作する必要がある。

- **実装方針**: `FileSystemDirectoryHandle` を IndexedDB に保存し、次回起動時に `queryPermission()` → 許可済みなら自動再オープン
- ブラウザのセキュリティポリシー上、ページロード直後の自動アクセスは不可。ワンクリックで再オープンできるボタン（「最近のフォルダ: xxx を開く」）として実装するのが現実的

---

## 5. GUI — 数式・computed フィールド

### 5-1. computed フィールドの式オーバーライドが GUI から設定しにくい ⚠️ **未対応**

`RichCell.override` が既に存在するセルはセル詳細パネルで編集できるが、
新規に override を追加する UI がない（`override` キー自体が存在しない場合、入力欄が表示されない）。

- 右クリック → 「式をオーバーライド」で `{ override: "" }` を初期化して編集させる

### 5-2. 数式エンジンの未対応関数 ⚠️ **部分対応**

#### 実装済み（core）
| 分類 | 関数・演算子 |
|------|------------|
| 算術 | `+` `-` `*` `/` `%` `()` |
| 比較 | `==` `!=` `<` `>` `<=` `>=` |
| 論理 | `AND` `OR` `NOT` |
| 条件 | `if(条件, 真, 偽)` |
| 集計 | `sum()` `avg()` `min()` `max()` `count()` |
| 文字列 | `concat()` `length()` |
| 参照 | `ref(テーブル名, idリスト, カラム名)` |

#### 未実装（一般的なスプレッドシート関数）
| 分類 | 関数 | 備考 |
|------|------|------|
| 数学 | `abs()` `round()` `floor()` `ceil()` `pow()` `sqrt()` | よく使う |
| 文字列 | `upper()` `lower()` `trim()` `substr()` `replace()` | |
| 論理 | `and(a, b, ...)` `or(a, b, ...)` （関数形式） | 演算子形式は実装済み |
| 型変換 | `int()` `float()` `str()` | |
| リスト | `contains(list, value)` `size(list)` | `list<int>` 型との連携 |

### 5-3. 数式エラーがバリデーション結果に含まれない ⚠️ **未対応**

computed フィールドの計算中に例外（ゼロ除算・未定義参照）が発生すると実行時エラーが投げられ、
バリデーション結果の `errors` には記録されない。

### 5-4. 循環参照の検出がない ⚠️ **未対応**

`formula: "effective_hp * 2"` のように computed フィールドが自分自身を参照すると無限ループになる。

---

## 6. GUI — その他 UX

### 6-1. ~~Undo / Redo がない~~ ✅ 対応済み（Ctrl+Z / Ctrl+Y、最大50件）

### 6-2. ~~リアルタイムバリデーションがない~~ ✅ 対応済み（300ms debounce、ツールバーバッジ表示）

### 6-3. ~~ソート・フィルタが未設定~~ ✅ 対応済み

### 6-4. 検索・置換がない ⚠️ **未対応**

Ctrl+F / Ctrl+H に対応する検索・置換 UI が未実装。

### 6-5. 列固定（freeze）がない ⚠️ **未対応**

`id` 列や `name` 列を固定して横スクロールしながら参照できない。

### 6-6. エクスポートボタンが GUI にない ⚠️ **未対応**

ツールバーに「エクスポート」ボタンが存在しない（CLI のみ）。

---

## 7. core ライブラリ — バグ・未実装

### 7-1. `list<int>` / `list<string>` のバリデーションが不完全 ⚠️ **未対応**

`max_length` がリストの要素数に対して機能していない。

### 7-2. `auto: increment` の自動採番が GUI 保存時に未実装 ⚠️ **部分対応**

行追加時に id を採番しているが、既存レコードの id が抜けている場合の補完は未対応。
また `version` フィールドの自動更新（`timestamp_version`）も未実装。

---

## 8. CLI — 不足機能

| コマンド | 問題 |
|--------|------|
| `new` | 作成テンプレートに `base_fields`（id/version）が含まれない |
| `diff` | DESIGN.md §2 に記載済みだが未実装 |
| `export` | protobuf / MessagePack が未実装（JSON・CSV のみ） |

---

## 9. GitHub Pages / デプロイ対応

- `vite.config.ts` に `base` パス設定なし（サブディレクトリでの動作が壊れる）
- GitHub Actions デプロイワークフロー未整備

---

## 10. 優先度まとめ

### P0 — ~~使えないレベルの問題~~ → すべて対応済み ✅

### P1 — UX を大きく損なう

1. 最後に開いたフォルダを記憶（4-1）
2. 右クリックから色・コメントを設定（3-1）
3. 複数セル・行のコピー（1-3）
4. コメントマーカー表示（3-2）
5. computed セルの式オーバーライド入力 UI（5-1）

### P2 — あると便利

6. セルを同じ値で埋める / フィルダウン（1-4）
7. 行の並び替え（2-3）
8. 検索・置換（6-4）
9. 列固定（6-5）
10. エクスポートボタン（6-6）

### P3 — 中長期

11. 数式エンジン追加関数（abs / round / contains 等）（5-2）
12. list 型バリデーション修正（7-1）
13. 数式エラーのバリデーション統合（5-3）
14. 循環参照検出（5-4）
15. protobuf / MessagePack エクスポーター（8）
16. GitHub Pages デプロイ（9）

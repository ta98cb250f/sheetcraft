# 課題・未実装一覧

SpreadSheet / Excel と比較した際の機能差分と、実装上の既知バグを整理する。

---

## 1. GUI — セル編集体験（最優先）

### 1-1. セル内直接編集ができない ⚠️ **致命的**

現状、セルをクリックしても下の「セル詳細パネル」に切り替わるだけで、グリッド上で直接キー入力できない。
AG Grid の `editable: true` は設定済みだが、ダブルクリックで起動するエディタが実際には機能していない可能性がある。

- **期待動作**: セルを選択してそのまま入力 or ダブルクリックでインライン編集
- **セル詳細パネル**: 廃止ではなく、色・コメント・式オーバーライドの編集専用として残す

### 1-2. コピー＆ペーストができない ⚠️ **致命的**

- セル → クリップボード → 別セル の貼り付けが効かない
- Excel / Google Sheets からのデータ貼り付けができない（Tab区切りテキストとして渡ってくる想定）
- AG Grid の `enableRangeSelection` / `enableCellTextSelection` が未設定

### 1-3. キーボードナビゲーションが機能していない

- `Tab` / `Shift+Tab` でセル移動
- `Enter` で確定して下へ
- `Escape` で編集キャンセル
- 方向キーでセル移動

AG Grid はデフォルトでサポートしているが、現在の設定では動作が不安定。

### 1-4. 範囲選択ができない

- 複数セル選択（Shift+クリック、Shift+矢印）ができない
- 現状 `rowSelection="single"` のみ設定

---

## 2. GUI — 行操作

### 2-1. 行の追加ができない（GUI上）

「新規行を追加」ボタンがない。空行をクリックして追加もできない。
現状は JSON を直接編集するしかない。

- **期待動作**: グリッド末尾の「＋」ボタン or `Tab` で最終セルを抜けたときに新規行追加

### 2-2. 行の削除ができない（GUI上）

右クリックメニューもDeleteキー操作も未実装。

### 2-3. 行の並び替えができない

ドラッグ＆ドロップによる行順変更未実装（AG Grid の `rowDragManaged` で対応可能）。

---

## 3. GUI — 一括操作・フィル

DESIGN.md の §9.4 に仕様として記載済みだが、**未実装**。

| 操作 | 状態 |
|------|------|
| フィルダウン（Ctrl+D） | 未実装 |
| フィルライト（Ctrl+R） | 未実装 |
| 連番フィル | 未実装 |
| パターンフィル（Excel風ドラッグ） | 未実装 |
| 検索・置換（Ctrl+F / Ctrl+H） | 未実装 |

---

## 4. GUI — 表示・UX

### 4-1. コメント付きセルの視覚的マーカーがない

DESIGN.md §9.3 では「右上に三角マーク（Excel風）」と定義されているが未実装。
`comment` が存在するセルを一目で識別できない。

### 4-2. フィールド型に応じた入力UIが未実装

| 型 | 現状 | 期待 |
|----|------|------|
| `enum` | テキスト表示（生値） | ドロップダウン選択 |
| `bool` | `true`/`false` 文字列 | チェックボックス |
| `list<int>` / `list<string>` | カンマ区切り文字列 | タグ入力 or 複数選択UI |
| `int` + `ref` | ID数値 | サジェスト付きルックアップ |

### 4-3. ソート・フィルタが未設定

AG Grid の `sortable` / `filter` オプションが有効化されていない。
列ヘッダークリックでのソートができない。

### 4-4. 列固定（freeze）がない

`id` 列や `name` 列を固定して横スクロールしながら参照できない。

### 4-5. リアルタイムバリデーションがない

「バリデーション」ボタンを押さないとエラーがわからない。
入力中 or 保存時に自動でバリデーションが走るべき。

### 4-6. Undo / Redo がない

DESIGN.md §12（継続検討）に記載済み。
誤操作の巻き戻しが一切できない。

### 4-7. エクスポートボタンがGUIにない

ツールバーに「エクスポート」ボタンが存在しない（CLI のみ）。
DESIGN.md の画面仕様（§9.2）には `[エクスポート]` が定義されている。

### 4-8. 列の表示/非表示切り替えができない

`export: false` の列（開発メモ等）を非表示にする操作が未実装。

### 4-9. 列幅の保存ができない

AG Grid で列幅を手動変更してもリロード時にリセットされる。

---

## 5. core ライブラリ — バグ・未実装

### 5-1. `list<int>` / `list<string>` のバリデーションが不完全

`max_length` がリストの要素数に対して機能していない。
`validation.ts` の `getCellValue()` がリストをそのまま返しているが、`string` / `number` チェックに入らないため min/max/max_length がスキップされる。

### 5-2. `ref_exists` が `validation` ブロック外では発動しない

フィールドに `validation` キーがない場合、`ref_exists` チェックに到達しない（`if (vr)` ブロック内にある）。

### 5-3. computed フィールドのバリデーションが未整備

数式エラー（ゼロ除算・未定義参照）が実行時例外として伝播し、バリデーション結果に含まれない。

### 5-4. 循環参照の検出がない

`formula: "effective_hp * 2"` のように computed フィールドが自分自身を参照すると無限ループになる。
DESIGN.md §12（継続検討）に記載済み。

### 5-5. `auto: increment` の自動採番が未実装

新規行追加時に `id` を自動で採番する処理がない。GUI・CLIどちらにも未実装。

---

## 6. CLI — 不足機能

| コマンド | 問題 |
|--------|------|
| `new` | 作成テンプレートに `base_fields`（id/version）が含まれない |
| `diff` | DESIGN.md §2 に記載済みだが未実装 |
| `export` | protobuf / MessagePack が未実装（JSON・CSV のみ） |

---

## 7. GitHub Pages / デプロイ対応

- `vite.config.ts` に `base` パス設定なし（サブディレクトリでの動作が壊れる）
- GitHub Actions デプロイワークフロー未整備

---

## 8. 優先度まとめ

### P0 — 使えないレベルの問題

1. セル内直接編集ができない（1-1）
2. コピー＆ペーストができない（1-2）
3. 行の追加・削除がGUIでできない（2-1、2-2）

### P1 — 著しくユーザー体験を損なう

4. キーボードナビゲーション（1-3）
5. enum / bool / list 型の入力UI（4-2）
6. リアルタイムバリデーション（4-5）
7. コメントマーカー表示（4-1）
8. Undo / Redo（4-6）

### P2 — あると便利

9. ソート・フィルタ有効化（4-3）
10. 一括フィル操作（3）
11. 列固定（4-4）
12. エクスポートボタン（4-7）
13. 検索・置換（3）

### P3 — 中長期

14. list<int> バリデーション修正（5-1）
15. 循環参照検出（5-4）
16. auto:increment 実装（5-5）
17. protobuf / MessagePack エクスポーター（6）
18. GitHub Pages デプロイ（7）

# 課題・未実装一覧

SpreadSheet / Excel と比較した際の機能差分と、実装上の既知バグを整理する。
マージ済みの対応を反映し、ユーザーフィードバックによる新規課題を追加。

---

## 1. GUI — セル編集体験

### 1-1. ~~セル内直接編集ができない~~ ✅ 対応済み

### 1-2. ~~コピー＆ペースト（単セル・TSV）~~ ✅ 対応済み（Ctrl+C / Ctrl+V）

### ~~1-3. 複数行の選択コピーができない~~ ✅ 対応済み

`rowSelection="multiple"` で複数行を選択し、Ctrl+C で全カラムを TSV 形式でコピー。

### ~~1-4. セルを同じ値で埋める機能がない~~ ✅ 対応済み

複数行を選択（Ctrl+クリック等）し Ctrl+D でフォーカス列の先頭行値を全選択行にフィルダウン。

### 1-5. Autofill（フィルハンドル） ⚠️ **未対応**

Excel のようにセル右下角をドラッグして値・数列・パターンを隣接セルへ展開する機能。
AG Grid Community では `enableFillHandle` が Enterprise 機能のため、カスタム実装が必要。

### 1-6. セルの範囲選択 ⚠️ **未対応**

Shift+クリックやドラッグで矩形範囲のセルを選択し、コピー・一括編集できるようにする。
AG Grid Community では `enableRangeSelection` が Enterprise 機能のため、カスタム実装が必要。
- 範囲選択 → Ctrl+C で TSV コピー
- 範囲選択 → Delete で一括クリア
- 範囲選択 → ペーストで矩形貼り付け

---

## 2. GUI — 行操作

### 2-1. ~~行の追加ができない~~ ✅ 対応済み（ツールバーボタン・右クリック・id 自動採番）

### 2-2. ~~行の削除ができない~~ ✅ 対応済み（Delete キー・右クリック）

### ~~2-3. 行の並び替えができない~~ ✅ 対応済み

AG Grid の `rowDragManaged` + `onRowDragEnd` で実装。先頭列にドラッグハンドル表示。ソート適用中はドラッグ無効（AG Grid 制約）。

---

## 3. GUI — 色・コメント（RichCell）操作

### ~~3-1. 色・コメントの入力が右クリックからできない~~ ✅ 対応済み

右クリック → 「色を設定」→ サブメニューで色選択。右クリック → 「コメントを編集」→ プロンプト入力。

### ~~3-2. コメント付きセルの視覚的マーカーがない~~ ✅ 対応済み

`cellRenderer` で右上に三角マーク（Excel 風）を描画。ホバーでコメント内容をツールチップ表示。

---

## 4. GUI — フォルダ・セッション管理

### ~~4-1. 最後に開いたフォルダを記憶できない~~ ✅ 対応済み

`FileSystemDirectoryHandle` を IndexedDB に保存。起動時に `queryPermission()` で許可済みなら自動再オープン。未許可時はツールバーに「再度開く: フォルダ名」ボタンを表示。

---

## 5. GUI — 数式・computed フィールド

### ~~5-1. computed フィールドの式オーバーライドが GUI から設定しにくい~~ ✅ 対応済み

computed フィールドを右クリック → 「式をオーバーライド」でプロンプト入力。空白で解除（`override: undefined`）。

### ~~5-2. 数式エンジンの未対応関数~~ ✅ 対応済み（追加実装）

#### 実装済み（core）
| 分類 | 関数・演算子 |
|------|------------|
| 算術 | `+` `-` `*` `/` `%` `()` |
| 比較 | `==` `!=` `<` `>` `<=` `>=` |
| 論理 | `AND` `OR` `NOT` / `and()` `or()` |
| 条件 | `if(条件, 真, 偽)` |
| 集計 | `sum()` `avg()` `min()` `max()` `count()` |
| 文字列 | `concat()` `length()` `upper()` `lower()` `trim()` `substr()` `replace()` |
| 数学 | `abs()` `round()` `floor()` `ceil()` `pow()` `sqrt()` |
| 型変換 | `int()` `float()` `str()` |
| リスト | `contains(list, value)` `size(list)` |
| 参照 | `ref(テーブル名, idリスト, カラム名)` |

### ~~5-3. 数式エラーがバリデーション結果に含まれない~~ ✅ 対応済み

`computeRecord` に式エラーコレクターを追加。ゼロ除算・未定義参照等の `FormulaError` をバリデーション errors に統合。

### ~~5-4. 循環参照の検出がない~~ ✅ 対応済み

`detectComputedCycles` で DFS サイクル検出を実装。循環参照のある computed フィールドをテーブルレベルエラーとして報告。

---

## 6. GUI — その他 UX

### 6-1. ~~Undo / Redo がない~~ ✅ 対応済み（Ctrl+Z / Ctrl+Y、最大50件）

### 6-2. ~~リアルタイムバリデーションがない~~ ✅ 対応済み（300ms debounce、ツールバーバッジ表示）

### 6-3. ~~ソート・フィルタが未設定~~ ✅ 対応済み

### ~~6-4. 検索・置換がない~~ ✅ 対応済み

Ctrl+F でフローティング検索パネルを表示。▲▼で移動、大小文字区別切替。Ctrl+H で置換モード追加（1件 / すべて置換）。

### ~~6-5. 列固定（freeze）がない~~ ✅ 対応済み

セルを右クリック → 「この列を左に固定」/ 「この列の固定を解除」で AG Grid の `pinned: 'left'` を動的切替。

### ~~6-6. エクスポートボタンが GUI にない~~ ✅ 対応済み

ツールバーに「JSON」エクスポートボタンを追加。computed フィールドを解決した状態でダウンロード。CSV エクスポートは削除済み。

---

## 7. core ライブラリ — バグ・未実装

### ~~7-1. `list<int>` / `list<string>` のバリデーションが不完全~~ ✅ 対応済み

`Array.isArray(value)` のチェックを追加し、`max_length` をリスト要素数に対して正しく検証するよう修正。

### 7-2. `auto: increment` の自動採番が GUI 保存時に未実装 ⚠️ **部分対応**

行追加時に id を採番しているが、既存レコードの id が抜けている場合の補完は未対応。
また `version` フィールドの自動更新（`timestamp_version`）も未実装。

### 7-3. バージョンの編集 ⚠️ **未対応**

`version` フィールドは現在 `auto` 扱いのため GUI から編集不可。
レコードを変更した際に version を手動でインクリメントする、または保存時に自動インクリメントする仕組みが必要。

**方針候補:**
- 保存時に変更のあったレコードの `version` を自動 +1 する（推奨）
- GUI から手動編集できるようにする（`auto` フラグを外す）

---

## 8. CLI — 不足機能

| コマンド | 問題 |
|--------|------|
| `new` | 作成テンプレートに `base_fields`（id/version）が含まれない |
| `diff` | DESIGN.md §2 に記載済みだが未実装 |
| `export` | ~~MessagePack が未実装~~ ✅ `-f msgpack` でバイナリ出力対応済み / protobuf は未実装 |

---

## 9. GUI — 列設定の編集

### 9-1. 列プロパティの編集 ⚠️ **未対応**

フィールド定義（`FieldDef`）は現状 JSON ファイルを直接編集しないと変更できない。列ヘッダーの右クリックまたは専用パネルから以下を編集・保存できるようにする。

| プロパティ | 内容 | 優先度 |
|---|---|---|
| `name` | 出力パラメータ名（英数） | 高 |
| `display_name` | グリッドのヘッダー表示名 | 高 |
| `export` | エクスポート対象フラグ | 高 |
| `formula` | computed フィールドの計算式 | 高 |
| `validation.min/max` | 数値の最小・最大 | 中 |
| `validation.max_length` | 文字数・リスト長の上限 | 中 |
| `validation.regex` | 正規表現バリデーション | 中 |
| `validation.unique` | 一意制約 | 中 |
| `anomaly.warn_above/below` | 異常値警告の閾値 | 中 |
| `editable` | GUI から編集可能かどうか | 低 |

`type`・`auto`・`enum_ref`・`ref` はスキーマ構造変更になるため対象外（JSON 直接編集）。

### 9-2. 列の追加・削除

GUI から `FieldDef` を新規追加・削除できるようにする。

**方針:**
- ツールバーまたは列ヘッダーの右クリックから「列を追加」
- 追加時に設定するプロパティ: `name`（必須）・`type`（必須）・`display_name`・`export` 等
- 削除時は既存レコードの該当フィールドデータも削除（非可逆操作のため確認ダイアログを表示）
- `type`・`name` は作成後の変更不可（スキーマ構造変更になるため）

**課題:**
- `name` の重複チェック・英数字制約のバリデーション
- `enum_ref` / `ref` / `auto` を持つ列の追加は複雑なため初期対応では対象外とし、JSON 直接編集を案内する

### 9-3. セル単位の式入力（`=` プレフィックス方式）

Excel ライクに、セルの値が `=` で始まる場合は式として評価する仕組み。

**方針:**
- **通常フィールド**: セルに `=formula` を入力するとそのセルだけ式化（現行の `RichCell.override` を拡張）
- **computed フィールド**: 列設定の「デフォルト式」は引き続き `FieldDef.formula` で管理。UI 上では `=` プレフィックス付きで表示・編集
- デフォルト式あり・セルが値で上書きされている状態は、現行の `override` の代わりにセル値が直接保存されることで表現

**課題:**
- 文字列フィールドでリテラルの `=` を入力したい場合のエスケープ（Excel 同様 `'=` 等）
- 式の評価結果の型がフィールド型と一致しない場合のバリデーション
- 列設定 GUI（9-1）と同時に設計・実装するのが望ましい

---

## 10. ~~GitHub Pages / デプロイ対応~~ ✅ 対応済み

- `vite.config.ts` に `base` パス設定追加（`GITHUB_ACTIONS` 環境変数で `/sheetcraft/` を適用）
- GitHub Actions デプロイワークフロー整備（`.github/workflows/deploy.yml`、`develop` ブランチ push で自動デプロイ）

---

## 11. 優先度まとめ

### P0 — ~~使えないレベルの問題~~ → すべて対応済み ✅

### P1 — ~~UX を大きく損なう~~ → すべて対応済み ✅

1. ~~最後に開いたフォルダを記憶（4-1）~~ ✅
2. ~~右クリックから色・コメントを設定（3-1）~~ ✅
3. ~~コメントマーカー表示（3-2）~~ ✅
4. ~~複数行のコピー（1-3）~~ ✅
5. ~~computed セルの式オーバーライド入力 UI（5-1）~~ ✅

### P2 — ~~あると便利~~ → すべて対応済み ✅

1. ~~行の並び替え（2-3）~~ ✅
2. ~~エクスポートボタン（6-6）~~ ✅
3. ~~フィルダウン Ctrl+D（1-4）~~ ✅
4. ~~検索・置換（6-4）~~ ✅
5. ~~列固定（6-5）~~ ✅

### P3 — 中長期（残件）

1. ~~数式エンジン追加関数（5-2）~~ ✅
2. ~~list 型バリデーション修正（7-1）~~ ✅
3. ~~数式エラーのバリデーション統合（5-3）~~ ✅
4. ~~循環参照検出（5-4）~~ ✅
5. ~~MessagePack エクスポーター（8）~~ ✅（msgpack 対応済み、protobuf は未対応）
6. ~~GitHub Pages デプロイ（10）~~ ✅
7. 列設定の編集 GUI（9） ⚠️ **未対応**
8. Autofill（1-5） ⚠️ **未対応**
9. セルの範囲選択（1-6） ⚠️ **未対応**
10. バージョンの編集（7-3） ⚠️ **未対応**

変更をコミットする。引数にコミットメッセージを渡す。

使い方: `/commit feat: 新機能を追加`

```bash
export PATH="$HOME/.nodebrew/node/v25.9.0/bin:$PATH"
git add -p
git commit -m "$ARGUMENTS

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

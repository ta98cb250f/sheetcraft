# 人的介入ログ

ユーザーが指摘・修正・誘導する必要があった事象を蓄積。Claude Code が単体で完結できる範囲を広げるための記録。

## 方針

- **追記タイミング**: セッション末 or PR 作成時、新しい順に上から積む
- **粒度**: 1 件 = 1 行を原則。詳細は PR / コミット / 該当ファイルから辿れるので書かない
- **書式**: `- [カテゴリ] 状況の要点 → 防止策`
- **カテゴリ**:
  - `指示違反` … CLAUDE.md / スキル / メモリ で定義済みのルールを守らなかった
  - `バグ` … 実装ミスで動かない・副作用
  - `仕様理解` … API / フレームワーク / データモデルの前提を誤って実装
  - `環境` … OS / 依存設定の前提ミス
  - `コミュニケーション` … 質問の曖昧さ・確認過多/不足・主張と検証の乖離
  - `プロセス` … コミット粒度・PR 段取り・検証段取り・自発性
- **対象外**: 1 ターンで解決した軽微な指示変更（言い回し調整など）
- **分析サイクル**: 10〜20 件 or 月次で傾向集計 → 頻出は CLAUDE.md / スキル / メモリ に昇格。昇格済みは行末に `→ <反映先>` を追記
- **過去ログの剪定**: ルール化された行は半年〜1年後に archive へ退避

## ログ

### 2026-05-18 — claude/review-pr-scope-WbuC8（範囲一括色付け 3-4 / 範囲ハイライト塗り色両立 3-5）

- [コミュニケーション] ユーザー指示「これまでの選択色と同じ色の枠、セル色で表現」を「枠線追加 + セル背景」と解釈し各セルに 2px solid 青枠を実装、ユーザーから「範囲選択のセル全部に枠がつくようになってます。いらないです」と指摘され削除する手戻り → UI 表現の指示で解釈が複数取れる場合、実装前に 1 行復唱で確認する → .claude/skills/echo-back-intent/
- [仕様理解] ISSUES.md 3-5「同色連続範囲の外周にだけ枠線を描画する」を額面通り受けて実装案に組み込んだが、ユーザーから「私の意図を誤って ISSUE に記載されている、無視して良い」と訂正 → ISSUES.md の既存記述を鵜呑みにせず、対応着手前にユーザー意図を 1 行で確認する
- [コミュニケーション] AskUserQuestion 後の着手宣言で確認項目を 6 グループ 20+ 件提示、ユーザーから「多すぎる」と指摘 → 事前判断が必須でない項目は実装者判断で進め、確認はユーザー判断が無いと方針が決まらないものに絞る
- [コミュニケーション] 「枠線色を詳しく」というユーザー返答を字面通り「2 案の詳細説明」と受け取って回答、本意「なぜ検討する必要があるのか」を読み違え → 短い返答が前のターンの何を指すか曖昧なときは、解釈を 1 行確認してから回答する → .claude/skills/echo-back-intent/
- [コミュニケーション] 「なぜ検討する必要があるか」の再質問に対し「事前判断は必須ではない」と返してはぐらかし、ユーザーから「いや、なぜ検討する必要があるのかを詳しく教えろと言っている」と強めの再指摘 → ユーザーが理由を求めているときは「不要」で逃げず、検討理由をきちんと言語化する
- [プロセス] `git push` を直前ターンで実行済みなのに完了を明示伝達せず、ユーザーから「だったらプッシュしたって言ってね」と指摘 → 不可逆操作（push / PR 作成等）が完了したら必ず「○○済み」と明示報告する → .claude/hooks/post-tool-use.py + CLAUDE.md §よくある介入 #7
- [仕様理解] applyCellColor を「範囲があれば常に範囲」で実装し、自分で書いたチェックリストでさえ「範囲外右クリックは単セル着色」と挙動が乖離。ユーザーから「正しく実装されていません」と指摘されて右クリック先が範囲内か判定する分岐に修正 → 自作チェックリストと実装が食い違う時点で実装の妥当性を疑い、Delete (右クリック概念なし) との挙動差を着手前に整理する
- [仕様理解] 塗り色 × 範囲色のブレンドを乗算 (multiplyHex) で実装、ユーザーから「乗算だと暗すぎる、間をとる」と指摘され平均 (mixHex) に修正 → ブレンド方式 (multiply / average / lighten 等) は複数案あり、明度を伴う合成は実装前に選択肢と試算サンプルを提示してから決める
- [コミュニケーション] セルフレビューでコメント L87 / L951 の修正方針を「修正→削除→ L951 は本 PR 触らない」と 2 回方針変更、ユーザーから「それが適切？」と 2 回問われた → レビュー指摘を出す前に CLAUDE.md「WHAT 説明禁止」「論点分離」を当てて方針を 1 回で組み立てる
- [コミュニケーション] レビュー結果を「L87 / L951 のコメント」と行番号だけで示し中身を書かず、ユーザー「L87 / L951 のコメントとは何？説明が不足」と指摘 → 行番号で示すときは Before / After のコード片を必ず添える
- [指示違反] 確認系メッセージ「この方針で良いですか？」に推奨対応を添えず、ユーザーから「対応を確認する際は推奨対応を添えてください」と指摘 → 既存メモリ `feedback_ask_user_recommended.md` は AskUserQuestion 限定の記述で、テキスト確認時に未適用だった。適用範囲を拡張する必要あり
- [指示違反] 推奨提示ルールを最初メモリ (`feedback_ask_user_recommended.md`) に置いていたが、ユーザーから「メモリではなくスキルにして」と指摘 → 既存メモリ `feedback_memory_scope.md` (プロジェクト固有はメモリでなくスキル) と同じ判断ミスを別形で再発、自発的にスキル化していなかった

### 2026-05-13 — claude/add-intervention-log（介入ログ分析と対策昇格）

- [コミュニケーション] AskUserQuestion で推奨案を最初に出さず中立に並べたため「推奨は？」が 4 回連続発生、ユーザーから「推奨毎回出せよ」と指摘 → `memory/feedback_ask_user_recommended.md`
- [コミュニケーション] hook 動作確認で別ターミナル新セッション起動をユーザーに依頼、「面倒です」と却下 → ユーザーに手動操作を求める前に Claude 側で完結する方法を先に検討する
- [プロセス] PreToolUse hook をメモリディレクトリへの書き込みに対し exit 2 (ブロック) で設計したため、直後のメモリ書き込み（ユーザー承認済み）も止まり実装中に exit 0 + stderr 警告へ再設計 → 監視 hook はユーザー承認後の正規パスを設計時に想定する
- [プロセス] 「順に進めて」を受けて「動作確認 → コミット → 追加対策」3 ステップを連続で走らせかけたが、CLAUDE.md §着手宣言「広い指示は最小 1 ステップで確認」を書いた直後だったため自己検知して確認を挟んだ（自己検知の成功例として記録）

### 2026-05-13 — feature/project-setup（環境整理・スキル提案）

- [指示違反] 「必要なスキルを提案して」を技術スタック (Node/pnpm/Vite 等) と解釈して提案、Claude Code skills のことと判らずユーザーから「あれ、スキルを提案してと言ったと思うのだけど」と訂正 → 「スキル」が曖昧な質問は技術スタック / Claude Code skills / MCP のどれかを最初に確認する → .claude/skills/echo-back-intent/
- [指示違反] 「進めて」の直後、依頼されていない package.json / pnpm-workspace.yaml / tsconfig.base.json / biome.json / packages 配下一式まで自走作成、ユーザーから「gitignore, CLAUDE.md 以外は依頼していません。破棄してください」と削除指示 → 「進めて」の対象範囲が広いときは最小 1 ステップだけ実行して確認に戻る
- [指示違反] メモリ `feedback_memory_confirm.md`（保存前にユーザー確認）に反し、`project_workflow.md` と `MEMORY.md` を無断作成 → メモリ書込みは確認を挟まなければ実行しない（再発） → .claude/hooks/pre-tool-use.py (WARN)
- [プロセス] `/fewer-permission-prompts` の結果が追加候補ゼロだったのに空の `.claude/settings.json` を新規作成 → 追加対象が無いときはファイル生成しない → .claude/hooks/pre-tool-use.py
- [プロセス] gh CLI 未インストールの可能性を確認せず `gh auth login` を案内、エラー後に `brew install gh` を後出し → 案内するコマンドの前提ツール存在を先に検証する → /pre-implement-check
- [プロセス] HTTPS push 認証失敗時、ユーザーが直後「いえ、ghで」と希望表明したのに、SSH 切替案を先に並列提示済みだった → ユーザーが手段を選んだら同類の代替案は撤回し選んだ経路に絞る
- [プロセス] `/init` → `/fewer-permission-prompts` の連続実行を事前合意なく開始 → 複数スキルを流す場合は順序と目的を提示してから進める
- [プロセス] 「Claude Code skills を追加で入れると良いもの」を聞かれた際、自分の認識（インストール可能 skills は無い / カスタム作成のみ）に確信が持てず、claude-code-guide で確認するまで一往復多く要した → 自インストールのデフォルト / カスタム内訳が曖昧な質問は最初に公式リファレンスを参照する
- [コミュニケーション] 「デフォルトであるものではなく、追加で入れるもの」と言われて MCP サーバーを提案、ユーザー意図は Claude Code skills の追加方法 → 「skills」明記の質問で勝手に MCP に話題を寄せない
- [コミュニケーション] 「環境を整理したい」の意図（モノレポセットアップ / Claude Code 設定 / ドキュメント整備）を最初に確認せず独自解釈で技術スタックを提案 → 範囲が広いトピックは選択肢を提示して合意してから進める
- [環境] Xcode license 未同意で git がブロックされ作業停止、ユーザーに外部ターミナルでの同意操作を依頼 → 新環境で git 初回コマンドが失敗したらエラー出力から前提条件を読み取って案内する

### 2026-05-13 — PR #14（人的介入ログ運用基盤の導入）

- [プロセス] 介入ログ第 1 版を 86 行で起こし、ユーザーから「情報もう少し詳細に」→「むしろ行数が多すぎ。100 件超を前提に」と粒度を 2 段階で修正された → 新規ログ系ファイルは「想定運用規模（件数・参照頻度）」を最初に確認してから書式を決める
- [プロセス] 介入ログ第 2 版で本セッション分を 7 件に絞り過小列挙、ユーザーから「もっと介入している。指示に従ってないとかのレベルから出して」と再列挙を要求された → 1 セッション 10+ 件が標準、自分の判断ミス・手戻り・自発性不足まで網羅する意識
- [指示違反] カテゴリ名を「コミュ」と略記、ユーザーから「カテゴリ『コミュ』略さないでください」と直接指摘 → 既存メモリ `feedback_communication_style.md`（造語/略語禁止）の遵守不足（再発） → .claude/hooks/stop.py
- [バグ] 「コミュ」→「コミュニケーション」の置換を Edit の `replace_all` で実施した結果、既に「コミュニケーション」と書かれていた箇所が「コミュニケーションニケーション」へ二重置換された → 前方一致が衝突する文字列で `replace_all` を使わない、置換前に対象パターンの全箇所を grep で確認する → .claude/hooks/pre-tool-use.py
- [プロセス] 新規 SKILL.md と運用ドキュメントを Write しただけで本文をユーザーに提示せず、ユーザーが IDE で開き直すまで内容レビューに入れなかった → 重要な新規ドキュメントは Write 後に骨子をインラインで提示してレビューを促す
- [プロセス] ブランチ戦略をユーザーに「ブランチは新しく作ってね」と明示誘導されるまで、本筋と無関係な運用系変更を既存の PR ブランチに積もうとした → コード本筋と異なる種類の変更（運用 / ドキュメント）は新ブランチに分けるかを最初に判断する

### 2026-05-13 — PR #7（pr-metadata スキル配置修正）

- [指示違反] `.claude/plugins/pr-metadata/` の配置を `settings.json` のマーケットプレイス登録だけ見て「正しい」と即答 → ユーザーから「.claude/skills じゃなかった？」と訂正され、claude-code-guide 経由で公式ドキュメントを参照して初めて誤りを認識 → 配置規約系は推測で断定せず最初に公式仕様を確認する
- [プロセス] 新ブランチでコミットに着手する直前、untracked の `packages/gui/.vite/` を ignore 対象として点検せず、ユーザーから「ignore したほうがいいものが乗っているか」と問われて初めて確認 → ブランチ切り直後 / コミット前に `git status` の untracked を毎回点検する → /pre-implement-check
- [バグ] pr-metadata スキルに書いた `gh milestone list` が `gh` に存在しないサブコマンドだった（PR #5 で導入した初版から残存、`gh api repos/.../milestones` が正しい）→ スキルに記載するコマンドは導入時に必ず一度実行確認する
- [プロセス] 新ブランチを develop からではなく古い feature ブランチ (`claude/review-implement-tasks-1QGJD`) のまま分岐させ、develop に既に入っていた SKILL.md 変更とリベース時にコンフリクト → 新ブランチは `git fetch && git checkout -b <name> origin/<default-branch>` で最新の base から切る
- [指示違反] PR 作成時に pr-metadata スキルを起動せず `gh pr create` を直接実行（Skill ツール経由の呼び出しは "Unknown skill" で失敗したまま手動でメタデータ適用）→ スキル起動が失敗した時点でユーザーに状況を伝え、手動代替するか中断するかを確認する

### 2026-05-12 — PR #13（範囲選択 1-6 + 関連バグ）

#### 指示違反・プロセス
- [指示違反] CLAUDE.md「実装方針」§1-3（API 仕様調査・影響範囲列挙・根拠を持って実装）を自主的に行わず、ユーザーから「影響するデータ形式と、処理の確認から進めてください」と明示誘導されて初めて実施
- [指示違反] 「対応前に答えて」と明示されるまで、ユーザーの質問に答えずに即修正に着手しようとした
- [プロセス] Shift+クリックを「実装した」と主張したが、実機で動作確認せず、ユーザーから「初耳ですが実装した？」と疑問を持たれた
- [プロセス] 範囲選択の初期実装でテーブル切替・列構造変化時の範囲解除が抜けており、コミット後の self-review で初めて追加
- [プロセス] Delete 修正をコミットしてから動作確認をユーザーに任せ、3 回「変わってません」と返ってきた（`suppressKeyboardEvent` → capture-phase Delete のみ → Backspace 追加）
- [プロセス] PR 作成時に develop との merge-base 差分を確認せず、`.claude/hooks/session-start.sh` 擬似削除等のノイズを混入。ユーザーの self-review 依頼で初めて気づいた
- [プロセス] Shift+クリック修正で capture-phase 案を最初提案し、ユーザーが「妥当ですか？」と検証要求 → より単純な ref ベース案を後出し（提案前の自己検証不足）
- [プロセス] コミット粒度: `df31c5b` で「CellDetailPanel rawCell 化」「computeRecord の "" 等価化」「Shift+クリック anchor 修正」の独立した論点を 1 コミットにまとめた
- [プロセス] Stop hook が発動して初めてコミット → 適切なタイミングで自発的にコミットしていない → /pre-commit-check
- [プロセス] 実装完了後に検証チェックリストを自発提示せず、ユーザーから「確認項目を10件以内で示して」と要求された → /pre-commit-check
- [プロセス] サンプルデータ (`example/master/character.json`) が私のバグ修正で破損したことを自分で気づかず、ユーザーの保存・確認で表面化
- [プロセス] dev server 起動・実機検証を自発的にせず、ユーザーから「確認したいので起動して」と依頼された

#### 技術的前提ミス
- [仕様理解] AG Grid のセル mousedown は React onMouseDown より先に focus を移すため `getFocusedCell()` が前のセルを返さない → 自前 `lastClickedCellRef` で anchor を track
- [バグ] `defaultColDef.suppressKeyboardEvent` は AG Grid のセルレベル listener より遅れて発火し効かないケースがある → grid root の capture-phase native listener を使う
- [環境] Mac の「delete」キーは `e.key === 'Backspace'` を送る → キー操作実装時は `Delete` と `Backspace` を両方扱う
- [環境] 新マシンでは `node` / `npm` が PATH 上になく `nodebrew` 経由でしか起動できない（既存スキルに記載済みだったが、最初は素の `npm` を叩いて失敗した）
- [仕様理解] formula 算出後の表示値を「保存値」として固定保存する誤実装をした → コンポーネント境界で「表示用 cell」と「保存値 rawCell」を別 prop で分離
- [仕様理解] 空文字 `""` の扱いを把握せず 4 段階の修正イテレーション（`cell === ''` → `rawCell === ''` → `rich.value === ''` → `computeRecord`）→ データモデルに触る修正は型定義 / シリアライズ / 表示 / バリデーション の各層を最初に grep する

#### コミュニケーション
- [コミュニケーション] AskUserQuestion の選択肢が「グリッド」「入力欄」「それ以外」と曖昧 → 「質問が曖昧なことに気をつけなさい」と直接指摘 → 観察可能な具体事象に限定する
- [コミュニケーション] formula 結果が表示され続けることを「消せないのが正しい」と曖昧な言い方で説明し、ユーザーを混乱させた → 「グリッド側」と「詳細パネル入力欄」のどちらの話かを毎回明示する
- [コミュニケーション] 介入ログの粒度を最初は冗長に書きすぎた（86 行）→ 100 件規模を前提とする視点が抜けていた
- [コミュニケーション] 介入ログ第 2 版でも本セッションの介入を過小列挙し、「もっと介入している」と再指摘された → 1 セッションで起きた手戻り・指摘・誘導を網羅的に拾う意識

### 2026-05-07 — PR #11（列設定 GUI / セル式入力 / computed 廃止 / 列幅・並び保存）

#### 指示違反・プロセス
- [指示違反] 「列の変数名をヘッダーに」を FieldEditModal 内で 4 回実装し続け、ユーザーが激怒「モーダルに出せなんて一言も言ってないでしょうが！！」 → CLAUDE.md §4 / find-component スキルへ反映
- [指示違反] `getMainMenuItems` を Community で使えると思い込んで実装→動作せず再実装 → CLAUDE.md §5 / check-agrid-community スキルへ反映
- [指示違反] メモリ「メモリ記録前に確認」を破り、無断でメモリファイルを 2 件作成 → 削除指示 → 「メモリへの更新は許可しない、スキルかCLAUDE.mdで」と再指示 → .claude/hooks/pre-tool-use.py (WARN)
- [プロセス] スキルを `.claude/commands/<name>.md` に誤配置 → ユーザー指摘で `.claude/skills/<name>/SKILL.md` に移行
- [プロセス] 「出ません」と返ってきても自己検証せず再実装を続けた → 「ていうかあなたは自分で確認できないの？」と指摘
- [プロセス] vite 開発サーバーのポートが起動毎に変わるのを「面倒」と指摘されるまで放置 → strictPort: 5173 固定
- [プロセス] 既存プロセスをキルせず dev server を起動 → kill→sleep→起動の手順を dev スキル化
- [プロセス] セル式 override の再編集時に計算値が初期値になる問題を自分で気づかずユーザーが指摘 → FormulaAwareTextEditor 追加
- [プロセス] ユーザーから「ビルド・テスト・起動を適宜スキル化して」と明示されるまでスキルを整備していなかった
- [プロセス] 「列の変数名」「placement」など指示を受けてから対象コンポーネントを find-component で確認せず、思い込みで実装着手

#### 技術的前提ミス
- [仕様理解] AG Grid v31 `getMainMenuItems` は型定義上は Community でも使えるが実体は Enterprise の MenuModule に依存 → 事前に dist の grep でモジュール参照を確認するルール化
- [仕様理解] AG Grid v31 の `cellDataType` 自動推論で int/float が `=expr` や `0.1.0` を NaN 化 → `cellDataType: false` で明示無効化
- [仕様理解] FieldDef.formula を `type === 'computed'` 限定で UI 表示していたが、`=` 式は任意のセルに使える前提が反映できていなかった → ユーザーから「computed 不要では」と設計指摘されて computed 型を廃止
- [仕様理解] `parseValue` が int/float 変換失敗時に文字列を fallback 保存していた → undefined を返して呼び出し側で破棄
- [仕様理解] AG Grid の `onColumnResized` / `onColumnMoved` はプログラム経由（flex 等）でも発火するため、ユーザー操作のみ保存するには `source === 'uiColumnXxx'` の判定が必要

#### コミュニケーション
- [コミュニケーション] HMR の略語を説明なしに使用 → 「略語使わないで」 → 既存メモリ「Communication style」§造語/略語禁止 の遵守不足（再発） → .claude/hooks/stop.py
- [コミュニケーション] 「型タグ」など Claude の独自造語で確認を求めてユーザーが「何ですか？」と返答 → 同上
- [コミュニケーション] 「placement / placeholder」の混同で意図の取り違え（カタカナ語の精度不足）
